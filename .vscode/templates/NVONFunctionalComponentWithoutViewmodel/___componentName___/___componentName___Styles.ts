import { createStyles, makeStyles } from "@material-ui/core";
import { CustomMuiTheme } from "../../../models/CustomMuiTheme";

export const use___ComponentName___Styles = makeStyles(
  (theme: CustomMuiTheme) => createStyles({})
);
